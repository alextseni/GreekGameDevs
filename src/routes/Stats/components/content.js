import React from 'react';
import { analytics } from 'utils';

const totals = [
  { key: 'totalGames', label: 'Total Games ' },
  { key: 'totalDevs', label: 'Total Dev Teams ' },
  { key: 'totalMedia', label: 'Total Media ' },
  { key: 'totalNetworks', label: 'Total Networks ' },
];

export default totals;
